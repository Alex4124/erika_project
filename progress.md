Original prompt: Сделай виртуальную лабораторию по химии, в которой будут стаканы в растворами веществ: соляная кислота, гидроксид калия, серная кислота, нитрат меди, нитрат серебра, хлорид бария, хлорид трёхвалентного железа, иодид калия, фенолфталеин, метилоранж, лакмус, карбонат лития, фосфат натрия, силикат калия, сульфид натрия, гидроксид кальция, пероксид водорода, перманганат калия. Каждый стакан должен быть подписан (название вещества), внешний вид каждого вещества должен соответствовать его реальным физическим свойствам. В лаборатории должны быть добавлены пробирки, в которые можно будет переливать реагенты, перетаскивая стакан с реагентом к пробирке. В случае возникновения реакции должна возникать анимация, соответствующая признакам и скорости реакции в реальности.

- Создан каркас проекта на Vite и автономная страница лаборатории.
- Добавлены 18 подписанных стаканов с визуально различимыми растворами и суспензиями.
- Реализованы 4 пробирки, переливание перетаскиванием и альтернативный выбор реагента кликом.
- Добавлен движок смешивания: кислотно-основная среда, индикаторы, осадки, газ, нагрев, желирование, обесцвечивание.
- Реализованы ключевые реакции: AgNO3 + KI, BaCl2 + H2SO4, Cu(NO3)2 + KOH, FeCl3 + KI, HCl + Li2CO3, H2SO4 + H2O2 + KMnO4 и другие.
- Добавлены `window.render_game_to_text` и `window.advanceTime(ms)` для автоматической проверки.
- Доработана укладка текста в карточках и canvas-дубле: длинные названия и формулы теперь остаются внутри ячеек.
- Усилены визуальные признаки реакций: более яркая смена цвета, контрастнее осадки, крупнее и заметнее пузырьки газа, сильнее подсветка и нагрев.
- Проверены через Playwright сценарии:
  - фенолфталеин + KOH: малиновое окрашивание;
  - AgNO3 + KI: жёлтый осадок;
  - H2SO4 + H2O2 + KMnO4: обесцвечивание с пузырьками и нагревом;
  - HCl + Li2CO3: газовыделение и нагрев.
- Дополнительно проверен стартовый экран после правки укладки текста.
- Артефакты проверок сохранены в `output/web-game/`.
- Замечание: Playwright-клиент навыка снимает именно canvas, поэтому визуал лаборатории продублирован на canvas-слой для корректных скриншотов.

- Аудит реакций расширен и перепроверен полным браузерным перебором всех пар реагентов через `window.__chemLabDebug`.
- Исправлена целевая реакция `Ca(OH)₂ + Li₂CO₃`: теперь срабатывает `calcium-carbonate` с белым осадком `CaCO₃`.
- Добавлены и проверены дополнительные видимые реакции:
  - `Ca(OH)₂ + H₂SO₄` -> белый осадок `CaSO₄`;
  - `FeCl₃ + Li₂CO₃` -> бурый осадок `Fe(OH)₃` и `CO₂`;
  - `Cu(NO₃)₂ + K₂SiO₃` -> голубовато-зелёный осадок силиката меди.
- Усилены визуальные признаки:
  - повышена яркость и насыщенность растворов, индикаторов, осадков и перекрывающих цветовых эффектов;
  - слой fallout для осадков переделан на направленное оседание сверху вниз;
  - для светлых осадков добавлен более контрастный холодный кант и шлейф.
- Для `calcium-carbonate` добавлено частичное просветление надосадочной жидкости, чтобы белый осадок лучше отделялся от мутного исходного раствора.
- Проверки после правок:
  - `node --check app.js` -> ok;
  - `npm run build` -> ok;
  - skill-client сценарии: `output/web-game/ag-ki-vivid-4`, `output/web-game/ag-ki-vivid-5`, `output/web-game/carbonate-vivid-4`, `output/web-game/indicator-vivid-3`;
  - debug-hook скриншоты: `output/web-game/calcium-carbonate-fall-4`, `output/web-game/calcium-carbonate-settle-4`, `output/web-game/calcium-sulfate-vivid-3`, `output/web-game/copper-silicate-vivid-3`, `output/web-game/iron-carbonate-vivid-3`, `output/web-game/permanganate-vivid-3`.
- Наблюдение по статическим кадрам: у белого `CaCO₃` направленное оседание слабее читается на одиночном стоп-кадре, чем у цветных осадков, но DOM-проверка подтверждает генерацию частиц fallout (десятки частиц на ранней фазе анимации).

- Добавлены новые цепочки реакций с продуктами уже выпавших осадков:
  - переходы `Ag₂O`, `Ag₂CO₃`, `Ag₃PO₄` в `AgCl` при добавлении `HCl`;
  - растворение в кислотах осадков `Cu₃(PO₄)₂`, `FePO₄`, `Ba₃(PO₄)₂`, `Ca₃(PO₄)₂`;
  - кислотное разложение осадков силикатов меди, бария и кальция с сохранением геля кремниевой кислоты.
- Для ОВР `KMnO₄ + KI` и `KMnO₄ + KI + H₂SO₄` усилены цветовые признаки: бурый оттенок йода теперь заметен в DOM и canvas, redox-слой и цветовая волна сделаны контрастнее.
- Рендер жидкости в пробирках переделан на трёхцветный градиент (`top/mid/bottom`) с более сильным учётом tint/overlay, поэтому изменения цвета после реакции читаются заметнее.
- После последних правок проверки:
  - `node --check app.js` -> ok;
  - `npm run build` -> ok;
  - skill-client: `output/web-game/skill-permanganate-redox-final`, `output/web-game/skill-carbonate-chain-final`, `output/web-game/skill-permanganate-redox-final-2`;
  - debug-hook сценарии с реальными паузами: `output/web-game/redox-chain-check-2`;
  - полный повторный аудит пар + кислота: `output/web-game/acid-chain-audit-2.json`.

- Интерфейс лаборатории переработан под выдвижные вкладки справа:
  - вкладка `Уравнения` показывает для каждой пробирки отдельный список прошедших реакций и соответствующие уравнения;
  - вкладка `Журнал` вынесена из нижней панели в тот же slide-out блок;
  - в нижней панели оставлена краткая подсказка о доступных вкладках.
- В логику интерфейса добавлены состояния `drawer`, переключение вкладок, закрытие по `Esc` и сериализация содержимого вкладок в `window.render_game_to_text()`.
- Для реакций собран отдельный словарь уравнений, чтобы в выдвижной панели отображались не только признаки, но и формулы происходящих превращений по каждой пробирке.
- Проверки после добавления вкладок:
  - `node --check app.js` -> ok;
  - `npm run build` -> ok;
  - skill-client прогон: `output/web-game/drawer-equations-skill`;
  - отдельная DOM-проверка вкладок и содержимого: `output/web-game/drawer-tabs-dom`.
- Замечание по верификации: skill-client по-прежнему снимает в первую очередь canvas-слой, поэтому для новых DOM-вкладок дополнительно использована полноэкранная Playwright-проверка со скриншотами панели уравнений и журнала.

- Скорректирован расчёт среды для солей, образованных слабыми основаниями и сильными кислотами: для `AgNO3`, `Cu(NO3)2` и `FeCl3` добавлена поправка на гидролиз по катиону, если катион не выведен в осадок активной реакцией.
- Индикаторы теперь корректно показывают кислую среду в таких растворах и после цепочек растворения осадков кислотой:
  - `метилоранж` -> красный;
  - `лакмус` -> красный;
  - `фенолфталеин` -> остаётся бесцветным с отдельным текстовым сообщением.
- Проверки после правки гидролиза:
  - `node --check app.js` -> ok;
  - `npm run build` -> ok;
  - обязательный `skill-client`: `output/web-game/indicator-hydrolysis-skill`;
  - DOM-проверка через debug API: `output/web-game/hydrolysis-indicators-dom`.
- Сценарии DOM-проверки подтвердили кислую среду для `Cu(NO3)2 + индикатор`, `FeCl3 + индикатор`, `AgNO3 + индикатор` и для цепочки `Cu(NO3)2 + KOH + HCl + индикаторы`.

- В карточки реагентов добавлены справочные hover/focus-подсказки: растворимость, `tпл`, `tкип`, сила электролита.
- Скорректирован внешний вид растворов под реальную растворимость:
  - бесцветные растворы (`HCl`, `KOH`, `H2SO4`, `AgNO3`, `BaCl2`, `KI`, `H2O2`, `Na3PO4`) сделаны заметно прозрачнее;
  - малорастворимые `Li2CO3` и `Ca(OH)2` оставлены более мутными;
  - слегка опалесцирующие и окрашенные растворы подправлены по `alpha/clarity/texture`.
- Во вкладке `Уравнения` к ионным уравнениям добавлены молекулярные; рендер переведён на группы `Молекулярное` и `Ионное`.
- Для `Журнала` и `Уравнений` включён рабочий скролл внутри feed-контейнеров; отдельным правилом зафиксировано скрытие неактивной панели по `[hidden]`, чтобы вкладки не накладывались друг на друга.
- По справочным данным использованы в первую очередь PubChem / PubChem PUG-View, а для неполных карточек дополнительно проверены `Methyl orange`, `Potassium metasilicate` и `Litmus` по внешним справочным страницам.
- Финальные проверки:
  - `node --check app.js` -> ok;
  - `npm run build` -> ok;
  - обязательный `skill-client`: `output/web-game/properties-skill-final`;
  - DOM-проверка tooltip, молекулярных уравнений, скролла и скрытия неактивных tabpanel: `output/web-game/properties-dom-verified`.
